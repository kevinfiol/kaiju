-- game variables
return {
  score = { p1 = 0, p2 = 0 },
  mouse = { x = 0, y = 0 },
  resize_scale = 3,
  gw = 180, -- base resolution width
  gh = 180, -- base resolution height
  sx = 1, -- scale applied to base resolution x axis
  sy = 1 -- scale applied to base resolution y axis
}